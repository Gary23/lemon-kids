# 项目协作约定

- 所有面向用户的回复使用简体中文。
- 所有应用、共享模块、云函数或数据服务的功能开发，必须遵循 [`docs/README.md`](docs/README.md) 的需求文档流程；不得跳过设计确认、自动测试、安装确认或人工测试确认的门禁。
- 对已经完成开发的分支进行纯 Git 合并、推送、回退、变基或分支维护，不属于功能开发流程，无需创建需求/设计/执行文档或等待设计确认；若处理合并冲突需要修改业务代码，或用户明确要求在合并时追加功能，则从该代码改动开始遵循需求文档流程。
- 每次需求只覆盖一个应用（或一个独立模块）范围。涉及多个应用时，按应用拆分为多个独立流程、独立文档目录和独立确认；共享模块、云函数、数据库等改动必须归入其受影响应用的流程，并在文档中注明依赖影响。
- 开始代码改动前，必须已获得该需求设计文档的明确批准；开始设备安装前，必须已完成自动测试并获得用户的安装确认；人工测试完成前，不得推送远程或标记需求完成。
- 在人工测试等待期间，用户提出新诉求时，若无法从描述明确判断是补充需求还是缺陷，必须主动询问；确认后分别按 `补充-` 或 `缺陷-` 文档流程重新执行。
- 修改 `cloud-functions/evaluate-reading/` 中会影响腾讯云 SCF 部署的源码、依赖或启动文件后，结束任务前必须生成一个新的可部署 ZIP。文件名使用 `evaluate-reading-web-YYYYMMDD-功能名.zip`，并保证 ZIP 根目录直接包含 `index.js`、`server.js`、`scf_bootstrap`、`package.json`、`package-lock.json` 和生产依赖 `node_modules/`。
- 生成部署 ZIP 后，必须用 `unzip -l` 核验其根目录结构，并在 `cloud-functions/evaluate-reading/README.md` 中更新当前部署包文件名。
